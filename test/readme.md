# open command line and run nweman

- open new command line (don't close the main process where the server is up).
- type: newman run Regression_Customers_rest_api.postman_collection.json. {and press Enter}

